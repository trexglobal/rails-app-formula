#!/bin/bash

timestamp=$(date +\%Y_\%m_\%d)

{% set reinforcement_node_ip = salt['mine.get']('reinforcement*.' + salt['grains.get']('circle') + '.*', 'network.ip_addrs').items()[0][1][0] %}

mongoexport -h {{ reinforcement_node_ip }}   -u admin --db trex_index --collection userdata  --query "{\"_id\": \"$1\"}" --out /srv/shared/backup/mongo/$1-${timestamp}.json
